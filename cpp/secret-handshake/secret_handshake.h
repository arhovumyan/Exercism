#pragma once
#include <string>
#include <vector>

namespace secret_handshake {

    // int converter(int number);
    std::vector<std::string> commands(int numbers);

}  // namespace secret_handshake
