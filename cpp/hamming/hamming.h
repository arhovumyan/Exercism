#pragma once
#include <string>

// you will take 2 strings and iterate through them one by one and count in case 
// chars of same indexes are not the same

namespace hamming {    
    int compute(const std::string& first, const std::string& second);
}  // namespace hamming
